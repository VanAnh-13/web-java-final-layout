export const environment = {
  production: true,
  apiUrl: '/api', // Production API URL (relative to domain)
  version: '1.0.0',
  authTokenKey: 'auth_token',
  refreshTokenKey: 'refresh_token',
  storagePrefix: 'balo_center_'
};
